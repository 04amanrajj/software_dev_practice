const Redis = require("ioredis");

const redis = new Redis();


// redis.set('name' , 'aman raj' ,'EX', 600).then(data => console.log({data}))

// let data = redis.ttl('name').then(data => console.log({data}))
